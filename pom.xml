<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.tibco.businessevents</groupId>
  <artifactId>contribution</artifactId>
  <version>1.0.0</version>

  <packaging>pom</packaging>

  <properties>
    <be.home>/path/to/be/home</be.home>
    <be.version>6.3.2</be.version>
    <junit.version>5.7.0</junit.version>
    <testcontainer.version>1.20.4</testcontainer.version>
    <maven.compiler.target>11</maven.compiler.target>
    <maven.compiler.source>11</maven.compiler.source>
    <mockito.version>5.23.0</mockito.version>
    <!-- Docker Engine API version used by Testcontainers' docker-java client -->
    <!-- during tests (see maven-surefire-plugin config). Modern Docker Engine -->
    <!-- rejects the docker-java default of 1.32. -->
    <dockerApiVersion>1.44</dockerApiVersion>
  </properties>

  <dependencyManagement>
    <dependencies>
        <!-- Common TIBCO BusinessEvents dependencies -->
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-common</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-common.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-kernel</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-kernel.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-channel-api</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
  		    <systemPath>${be.home}/lib/cep-channel-api.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-base</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-base.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-drivers</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-drivers.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-ui-rt-common</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-ui-rt-common.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibcrypt</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/TIBCrypt.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibcort</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/TIBCOrt.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibxml-sdk</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/com.tibco.tibcoxml.xml.sdk.eclipse.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibxml-util</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/com.tibco.tibcoxml.util.eclipse.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibxml-io</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/com.tibco.tibcoxml.io.eclipse.jar</systemPath>
        </dependency>
        <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-emf-ecore</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tpcl/emf/org.eclipse.emf.ecore.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-emf-common</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tpcl/emf/org.eclipse.emf.common.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-emf-ecore-xmi</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tpcl/emf/org.eclipse.emf.ecore.xmi.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-external-tibcoxml-wsdl</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/com.tibco.tibcoxml.xml.ws.wsdl.eclipse.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-store</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-store.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>cep-backingstore</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/cep-backingstore.jar</systemPath>
        </dependency>
	      <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>be-functions</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/be-functions.jar</systemPath>
        </dependency>
		    <dependency>
            <groupId>com.tibco.businessevents</groupId>
            <artifactId>api</artifactId>
            <version>${be.version}</version>
            <scope>system</scope>
            <systemPath>${be.home}/lib/ext/tibco/org.genxdm.api.jar</systemPath>
        </dependency>

        <!-- End of common TIBCO BusinessEvents dependencies -->
		
        <!-- Other common dependencies like junit/testcontainers/etc-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
              <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
              <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
      <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
              <version>${junit.version}</version>
            <scope>test</scope>
      </dependency>
        <!-- mockito-core (5.x) supplies the inline mock-maker by default, which is  -->
        <!-- required for mockStatic(...). The separate mockito-inline artifact was   -->
        <!-- discontinued after 5.2.0. 5.x bundles a ByteBuddy that can instrument    -->
        <!-- modern (Java 17+/25) class files, unlike the previous 3.7.0 stack.       -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
              <version>${testcontainer.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
              <version>${testcontainer.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- ============================================================== -->
        <!-- Transitive-vulnerability pins (Dependabot remediation).        -->
        <!-- These force known-vulnerable transitive libraries pulled in by -->
        <!-- module dependencies (AWS SDK, Kafka, Lettuce, ES client, etc.) -->
        <!-- up to fixed versions repo-wide. Modules that declare these     -->
        <!-- directly with an explicit <version> still override these.      -->
        <!-- ============================================================== -->
        <dependency>
            <groupId>com.fasterxml.jackson</groupId>
            <artifactId>jackson-bom</artifactId>
            <version>2.17.2</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-bom</artifactId>
            <version>4.1.115.Final</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>33.3.1-jre</version>
        </dependency>
        <!-- Align all AWS SDK v1 artifacts (incl. transitive cloudwatch/dynamodb/ -->
        <!-- kms/s3 pulled by the Kinesis client) to a single fixed 1.12.x.        -->
        <dependency>
            <groupId>com.amazonaws</groupId>
            <artifactId>aws-java-sdk-bom</artifactId>
            <version>1.12.788</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
        <!-- Align BouncyCastle (incl. bcpkix/bcutil pulled transitively by      -->
        <!-- OpenSAML) to a fixed, CVE-free version. 1.84 clears GHSA-574f-3g2m- -->
        <!-- x479 (critical) and GHSA-c3fc-8qff-9hwx (moderate).                 -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.84</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.84</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcutil-jdk18on</artifactId>
            <version>1.84</version>
        </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <!-- Strip bundled-jar signatures. Shade rewrites MANIFEST.MF,   -->
              <!-- which invalidates signatures from signed deps (e.g. Bouncy   -->
              <!-- Castle bcprov) and causes a SecurityException at class-load. -->
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
              <artifactSet>
                <excludes>
                  <!-- Duplicate BE jars -->
                  <exclude>com.tibco.*:cep*</exclude>
                  <!-- Duplicate/conflicting tpcl jars already shipped under -->
                  <!-- BE_HOME/lib/ext/tpcl. Excluded from the uber-jar to   -->
                  <!-- avoid class-loading/version-skew conflicts at runtime. -->
                  <exclude>com.fasterxml.jackson.core:jackson-core</exclude>
                  <exclude>com.fasterxml.jackson.core:jackson-databind</exclude>
                  <exclude>com.fasterxml.jackson.core:jackson-annotations</exclude>
                  <exclude>com.fasterxml.jackson.dataformat:jackson-dataformat-cbor</exclude>
                  <exclude>org.apache.logging.log4j:log4j-api</exclude>
                  <exclude>org.apache.logging.log4j:log4j-core</exclude>
                  <exclude>org.apache.httpcomponents:httpclient</exclude>
                  <exclude>org.apache.httpcomponents:httpcore</exclude>
                  <exclude>org.apache.httpcomponents:httpcore-nio</exclude>
                  <exclude>org.apache.httpcomponents:httpasyncclient</exclude>
                  <exclude>commons-httpclient:commons-httpclient</exclude>
                  <exclude>commons-codec:commons-codec</exclude>
                  <exclude>commons-logging:commons-logging</exclude>
                  <exclude>com.google.guava:guava</exclude>
                  <exclude>io.netty:*</exclude>
                  <exclude>org.slf4j:slf4j-api</exclude>
                </excludes>
              </artifactSet>
            </configuration>
          </execution>
        </executions>
    </plugin>
    
	  <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.22.1</version>
        <configuration>
          <!-- Testcontainers' docker-java client otherwise talks Docker API     -->
          <!-- v1.32, which modern Docker Engine (>= v28, min API 1.44) rejects  -->
          <!-- with HTTP 400. docker-java takes the API version from the          -->
          <!-- 'api.version' system property; pin a supported value for the       -->
          <!-- forked test JVM. Override with -DdockerApiVersion=... if needed.   -->
          <systemPropertyVariables>
            <api.version>${dockerApiVersion}</api.version>
          </systemPropertyVariables>
          <!-- Mirror the JDK module opens that BE's runtime sets in be-engine.tra, -->
          <!-- so tests exercising BE engine code (e.g. its XML factories) work on   -->
          <!-- a modern (module-encapsulated) JDK instead of failing with            -->
          <!-- InaccessibleObject/IllegalAccess errors.                              -->
          <argLine>
            --add-opens=java.base/java.io=ALL-UNNAMED
            --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
            --add-opens=java.base/java.lang=ALL-UNNAMED
            --add-opens=java.base/java.math=ALL-UNNAMED
            --add-opens=java.base/java.net=ALL-UNNAMED
            --add-opens=java.base/java.nio=ALL-UNNAMED
            --add-opens=java.base/java.text=ALL-UNNAMED
            --add-opens=java.base/java.time=ALL-UNNAMED
            --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED
            --add-opens=java.base/java.util.concurrent.locks=ALL-UNNAMED
            --add-opens=java.base/java.util.concurrent=ALL-UNNAMED
            --add-opens=java.base/java.util=ALL-UNNAMED
            --add-opens=java.base/jdk.internal.access=ALL-UNNAMED
            --add-opens=java.base/jdk.internal.misc=ALL-UNNAMED
            --add-opens=java.base/sun.net.util=ALL-UNNAMED
            --add-opens=java.base/sun.net=ALL-UNNAMED
            --add-opens=java.base/sun.nio.ch=ALL-UNNAMED
            --add-opens=java.base/sun.reflect.generics.reflectiveObjects=ALL-UNNAMED
            --add-opens=java.base/sun.security.ssl=ALL-UNNAMED
            --add-opens=java.base/sun.util.calendar=ALL-UNNAMED
            --add-opens=java.desktop/java.awt.font=ALL-UNNAMED
            --add-opens=java.management/com.sun.jmx.mbeanserver=ALL-UNNAMED
            --add-opens=java.management/sun.management=ALL-UNNAMED
            --add-opens=java.sql/java.sql=ALL-UNNAMED
            --add-opens=java.xml/com.sun.org.apache.xalan.internal.xsltc.trax=ALL-UNNAMED
            --add-opens=java.xml/com.sun.org.apache.xerces.internal.jaxp=ALL-UNNAMED
            --add-opens=java.xml/com.sun.xml.internal.stream=ALL-UNNAMED
            --add-opens=jdk.internal.jvmstat/sun.jvmstat.monitor=ALL-UNNAMED
            --add-opens=jdk.management/com.sun.management.internal=ALL-UNNAMED
          </argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!-- sub modules -->
  <modules>
    <module>channel</module>
    <module>catalog</module>
    <module>metric</module>
    <module>store</module>
  </modules>

</project>
